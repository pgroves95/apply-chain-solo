import {FIRST_NAME_INPUT,
    LAST_NAME_INPUT,
    EMAIL_INPUT,
    PHONE_INPUT,
    EXTENDED_INPUT_1,
    EXTENDED_INPUT_2,
    SUBMIT_FORM
    } from './action-types'

const handleInputChange = (action, payload) {
    switch(action.type){
        case FIRST_NAME_INPUT:
            
    }
}